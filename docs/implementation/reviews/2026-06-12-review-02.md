# 재리뷰: 멀티 에이전트 지원 수정 검증 (2026-06-12 review-02)

## 대상

[review-01](2026-06-12-review-01.md)에서 지적한 6개 항목(H1, M1, M2, M3, L1, L3)의 수정 반영 검증. 수정 커밋 `1be4894 fix(installer): address review findings in install and test scripts`.

> 본 문서는 리뷰 기록이며 코드를 수정하지 않았다.

## 검증 결과 — 전 항목 정상 반영

| 항목 | 상태 | 검증 근거 |
|---|---|---|
| H1 | ✅ | `release_package_tools.sh:102,135` 가 `specs/agent/unreal-automation-agents.md` 로 변경. `specs/codex` 잔존 0건. |
| M1 | ✅ | `uecommandforge_speed_baseline.sh:4` `AGENT_HOME_VAL="${AGENT_HOME:-${CODEX_HOME:-$HOME/.codex}}"` — `AGENT_HOME` 최우선, 레거시 fallback 유지. |
| M2 | ✅ | `release_package_install.sh`: 옛 마커 블록 seed(`:182-185`) → `--codex` 재설치 → 옛 마커 0(`:216`)·새 마커 1(`:217`) 단언. env 파일 source 후 `AGENT_HOME`/`UECF_PROJECT_FILE` 값 검증(`:209-211`). 관리 블록 크기 `-le 30`(`:214-215`) 복원. |
| M3 | ✅ | plan/spec 문서에 `Saved/CodexReports`·`TEXT("CodexReports")` 리터럴이 정상 복귀(변경 전/후 구분 회복). 손상(before==after) 흔적 없음. |
| L1 | ✅ | `install_local.sh:143,148,151` `has_agent_state` 로 리네임. `has_codex_state` 잔존 0. |
| L3 | ✅ | `RUN_COMMANDLET_CHECK`/`hello.sh` 가 for 루프 **밖**(`:613-619`)으로 이동, `FIRST_AGENT` 기준 1회만 실행. |

L2(stdout 캡처 무결성)는 review-01에서 조치 불요(확인 권고)였고, 회귀 없음.

## 구문 검사

`install_local.sh`, `release_package_install.sh`, `release_package_tools.sh`, `uecommandforge_speed_baseline.sh` 전부 `bash -n` 통과.

## 잔여 관찰 (비차단, 신규 finding 아님)

- `install_local.sh`: `RUN_COMMANDLET_CHECK` 블록(`:614`)과 프로젝트 manifest 블록(`:616~`)이 각각 `FIRST_AGENT`/`FIRST_AGENT_HOME`/`FIRST_INSTALL_ROOT` 를 중복 계산한다. 두 경로 모두 사용 전 정의하므로 버그는 아니며, 공통 1회 계산으로 정리하면 깔끔(선택).

## 결론

review-01의 모든 finding이 검증 가능하게 해소됐다. CRITICAL/HIGH 잔여 0. 머지 차단 사유 없음.

> 권고: 설치/언인스톨/경로 가드 변경이 포함되므로 머지 전 UE 엔진 환경에서 smoke 스위트 1회 실행(`Saved/UECommandForge/Reports` 생성·마이그레이션 케이스 PASS 확인) 및 보안 diff 리뷰 1회 권장.
